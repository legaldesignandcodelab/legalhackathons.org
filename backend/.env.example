DJANGO_SECRET_KEY=change-me-to-a-long-random-string
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
DB_PASSWORD=postgres

# Email — leave blank to use console backend (prints to terminal) during development
# For production: set EMAIL_BACKEND=anymail and fill Resend API key
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
RESEND_API_KEY=

# From address for confirmation emails
DEFAULT_FROM_EMAIL=Legal Hackathon <legal-hackathon@unisg.ch>

# Base URL for QR code links (no trailing slash)
SITE_BASE_URL=http://localhost

# Admin check-in page password (simple JS gate)
CHECKIN_PASSWORD=hackathon2026
